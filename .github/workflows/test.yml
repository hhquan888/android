name: CloudPhone

on:
  workflow_dispatch:
    inputs:
      android_version:
        description: "Android version"
        required: true
        default: "12"
        type: choice
        options:
          - "9"
          - "10"
          - "11"
          - "12"
      gapps:
        description: "Install Google Play (Gapps)"
        required: true
        default: "y"
        type: choice
        options:
          - "y"
          - "n"
      root:
        description: "Turn on root, 1 means yes, 0 means no."
        required: true
        default: "0"
        type: choice
        options:
          - "0"
          - "1"
      arch:
        description: "ARM64 and AMD64 CPUs; ARM64 is recommended for better performance."
        required: true
        default: "amd64"
        type: choice
        options:
          - "amd64"
          - "arm64"
      ram_gb:
        description: "RAM limit for virtual machine (GB)"
        required: true
        default: "4"
        type: choice
        options:
          - "2"
          - "4"
          - "8"
          - "12"
      save_data:
        description: "Save virtual machine data, Y means yes, N means no (if you choose N, the data will always be lost when you run a new virtual machine)."
        required: true
        default: "n"
        type: choice
        options:
          - "y"
          - "n"

jobs:
  cloudphone:
    runs-on: ${{ github.event.inputs.arch == 'arm64' && 'ubuntu-24.04-arm' || 'ubuntu-24.04' }}
    timeout-minutes: 350

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Tinh toan image Redroid + bien kien truc
        run: |
          ANDROID_VER="${{ github.event.inputs.android_version }}"
          GAPPS="${{ github.event.inputs.gapps }}"
          ROOT="${{ github.event.inputs.root }}"
          ARCH="${{ github.event.inputs.arch }}"
          RAM="${{ github.event.inputs.ram_gb }}"

          if [ "$GAPPS" = "y" ] && [ "$ROOT" = "1" ]; then
            IMAGE_TAG="furtif/redroid:${ANDROID_VER}.0.0-rooted-gapps"
          elif [ "$GAPPS" = "y" ] && [ "$ROOT" = "0" ]; then
            if [ "$ANDROID_VER" -ge 11 ]; then
              IMAGE_TAG="furtif/redroid:${ANDROID_VER}.0.0-gapps"
            else
              IMAGE_TAG="furtif/redroid:${ANDROID_VER}.0.0-rooted-gapps"
            fi
          elif [ "$GAPPS" = "n" ] && [ "$ROOT" = "1" ]; then
            IMAGE_TAG="furtif/redroid:${ANDROID_VER}.0.0-rooted"
          else
            IMAGE_TAG="furtif/redroid:${ANDROID_VER}.0.0"
          fi

          if [ "$ARCH" = "arm64" ]; then
            ABILIST="arm64-v8a,armeabi-v7a,armeabi"
            ABILIST64="arm64-v8a"
            ABILIST32="armeabi-v7a,armeabi"
            NDK_TRANSLATION=""
            CLOUDFLARED_BIN="cloudflared-linux-arm64"
          else
            ABILIST="x86_64,x86,arm64-v8a,armeabi-v7a,armeabi"
            ABILIST64="x86_64,arm64-v8a"
            ABILIST32="x86,armeabi-v7a,armeabi"
            NDK_TRANSLATION="ro.dalvik.vm.native.bridge=libndk_translation.so"
            CLOUDFLARED_BIN="cloudflared-linux-amd64"
          fi

          CPU_COUNT=$(nproc)
          if [ "$CPU_COUNT" -ge 4 ]; then CPU_LIMIT=4; else CPU_LIMIT=$CPU_COUNT; fi

          echo "IMAGE_TAG=$IMAGE_TAG"             >> $GITHUB_ENV
          echo "ABILIST=$ABILIST"                 >> $GITHUB_ENV
          echo "ABILIST64=$ABILIST64"             >> $GITHUB_ENV
          echo "ABILIST32=$ABILIST32"             >> $GITHUB_ENV
          echo "NDK_TRANSLATION=$NDK_TRANSLATION" >> $GITHUB_ENV
          echo "CLOUDFLARED_BIN=$CLOUDFLARED_BIN" >> $GITHUB_ENV
          echo "CPU_LIMIT=$CPU_LIMIT"             >> $GITHUB_ENV
          echo "RAM_GB=$RAM"                      >> $GITHUB_ENV

          echo "Image: $IMAGE_TAG | ABI: $ABILIST | NDK: ${NDK_TRANSLATION:-none} | CF: $CLOUDFLARED_BIN | CPU: $CPU_LIMIT"

      - name: Khoi phuc du lieu may ao (neu bat luu)
        if: ${{ github.event.inputs.save_data == 'y' }}
        uses: actions/cache@v4
        with:
          path: data
          key: cloudphone-data-android${{ github.event.inputs.android_version }}-${{ github.event.inputs.arch }}
          restore-keys: |
            cloudphone-data-android${{ github.event.inputs.android_version }}-${{ github.event.inputs.arch }}

      - name: Cai dat cong cu can thiet
        run: |
          sudo apt-get update
          sudo apt-get install -y android-tools-adb curl
          sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" \
            -o /usr/local/bin/docker-compose
          sudo chmod +x /usr/local/bin/docker-compose
          docker-compose --version

      - name: Tai module kernel binder
        run: |
          sudo apt-get install -y "linux-modules-extra-$(uname -r)" || true
          sudo modprobe binder_linux devices="binder,hwbinder,vndbinder" \
            || sudo modprobe binderfs \
            || echo "::warning::binder khong load duoc"
          sudo modprobe ashmem_linux || true
          lsmod | grep -E "binder|ashmem" || echo "(khong co module nao khop)"

      - name: Tai ws-scrcpy va chuan bi Dockerfile
        run: |
          mkdir -p ws-scrcpy-build
          curl -L "https://github.com/hhquan888/ws-scrcpy/releases/download/1.0/ws-scrcpy-dist.zip" \
            -o ws-scrcpy-dist.zip
          EXPECTED="5fedd7aa159a7696689cf943abc4c080518bab2b44d94cf5ebe3bf585a540d24"
          ACTUAL=$(sha256sum ws-scrcpy-dist.zip | awk '{print $1}')
          if [ "$ACTUAL" != "$EXPECTED" ]; then
            echo "sha256 KHONG khop: $ACTUAL"
            exit 1
          fi
          unzip -o ws-scrcpy-dist.zip -d ws-scrcpy-build
          cp .github/scripts/Dockerfile.scrcpy ws-scrcpy-build/Dockerfile
          cat ws-scrcpy-build/Dockerfile

      - name: Tao docker-compose.yml
        run: |
          mkdir -p data
          python3 .github/scripts/gen-compose.py
          echo "=== docker-compose.yml ==="
          cat docker-compose.yml

      - name: Khoi chay Redroid va scrcpy-web
        run: |
          docker-compose up -d
          echo "Doi containers khoi dong (30s)..."
          sleep 30
          docker ps -a

      - name: "[CHAN DOAN] Log tat ca container"
        run: |
          for name in $(docker ps -a --format '{{.Names}}'); do
            echo "===== $name ====="
            docker logs "$name" 2>&1 | tail -n 100
            docker inspect "$name" --format "$name: {{.State.Status}} (restarts: {{.RestartCount}})"
          done

      - name: Kiem tra ADB va boot hoan tat
        run: |
          adb connect localhost:5555 || true
          adb devices -l
          for i in $(seq 1 36); do
            BOOTED=$(adb -s localhost:5555 shell getprop sys.boot_completed 2>/dev/null | tr -d '\r')
            if [ "$BOOTED" = "1" ]; then echo "Android da boot xong!"; break; fi
            echo "Doi boot... ($i/36)"
            sleep 10
          done
          STATE=$(adb -s localhost:5555 get-state 2>/dev/null || echo "unknown")
          echo "ADB state: $STATE"
          [ "$STATE" != "device" ] && echo "::warning::ADB chua o trang thai device" || true

      - name: Bat root (neu chon root=1)
        if: ${{ github.event.inputs.root == '1' }}
        run: |
          adb -s localhost:5555 root
          sleep 3
          adb connect localhost:5555 || true
          adb -s localhost:5555 remount || echo "remount that bai (binh thuong)"
          adb -s localhost:5555 shell whoami

      - name: Kiem tra abilist va thong tin he thong
        run: |
          adb -s localhost:5555 shell getprop ro.product.cpu.abilist
          adb -s localhost:5555 shell getprop ro.build.version.release
          adb -s localhost:5555 shell getprop ro.dalvik.vm.native.bridge || echo "(khong co)"
          adb -s localhost:5555 shell cat /proc/meminfo | head -n 3
          docker inspect redroid --format "Memory: {{.HostConfig.Memory}} | CPUs: {{.HostConfig.NanoCpus}}"

      - name: Kiem tra Google Play Store
        run: |
          if adb -s localhost:5555 shell pm list packages 2>/dev/null | grep -q "com.android.vending"; then
            echo "Google Play Store da cai."
          else
            echo "Khong tim thay Google Play Store."
            if [ "${{ github.event.inputs.gapps }}" = "y" ]; then
              echo "::warning::GApps duoc chon nhung Play Store khong co."
              adb -s localhost:5555 shell pm list packages | head -30
            fi
          fi

      - name: Cai Brave neu chua co browser
        run: |
          if adb -s localhost:5555 shell pm list packages 2>/dev/null | grep -qE "chrome|firefox|brave"; then
            echo "Da co trinh duyet, bo qua."
          else
            curl -L "https://github.com/brave/brave-browser/releases/download/v1.93.132/Bravearm64Universal.apk" \
              -o brave.apk
            adb -s localhost:5555 install -r brave.apk && echo "Brave installed" || echo "Cai Brave that bai."
          fi

      - name: Kiem tra scrcpy-web san sang
        run: |
          for i in $(seq 1 20); do
            CODE=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8000 || echo "000")
            if [ "$CODE" = "200" ]; then echo "scrcpy-web san sang"; break; fi
            echo "Doi scrcpy-web... ($i/20) [HTTP $CODE]"
            sleep 5
          done

      - name: Cai va chay upload server
        run: |
          mkdir -p uploads && sudo chmod 777 uploads
          mkdir -p upload-server
          cd upload-server
          npm init -y >/dev/null 2>&1
          npm install express multer >/dev/null 2>&1
          cp ../.github/scripts/upload-server.js server.js
          cd ..
          nohup node upload-server/server.js > upload-server.log 2>&1 &
          disown
          sleep 5
          curl -s http://localhost:3000/list > /dev/null && echo "Upload server OK" || cat upload-server.log

      - name: Cai Cloudflare Tunnel (co watchdog)
        run: |
          curl -L "https://github.com/cloudflare/cloudflared/releases/latest/download/${CLOUDFLARED_BIN}" \
            -o cloudflared
          chmod +x cloudflared && sudo mv cloudflared /usr/local/bin/
          pkill cloudflared || true
          > cloudflared.log
          > cloudflared-upload.log

          nohup cloudflared tunnel --url http://localhost:8000 >> cloudflared.log 2>&1 &
          disown
          nohup cloudflared tunnel --url http://localhost:3000 >> cloudflared-upload.log 2>&1 &
          disown

          for i in $(seq 1 20); do
            sleep 3
            URL=$(grep -o 'https://[a-zA-Z0-9.-]*\.trycloudflare\.com' cloudflared.log | head -n1 || true)
            if [ -n "$URL" ]; then echo "Tunnel chinh: $URL"; echo "TUNNEL_URL=$URL" >> $GITHUB_ENV; break; fi
          done
          for i in $(seq 1 20); do
            sleep 2
            UP=$(grep -o 'https://[a-zA-Z0-9.-]*\.trycloudflare\.com' cloudflared-upload.log | head -n1 || true)
            if [ -n "$UP" ]; then echo "Tunnel upload: $UP"; echo "UPLOAD_URL=$UP" >> $GITHUB_ENV; break; fi
          done

          chmod +x .github/scripts/cf-watchdog.sh
          nohup .github/scripts/cf-watchdog.sh > cloudflared-watchdog.log 2>&1 &
          disown

      - name: In huong dan su dung
        run: |
          echo "=========================================="
          echo "Android ${{ github.event.inputs.android_version }} | GApps: ${{ github.event.inputs.gapps }} | Root: ${{ github.event.inputs.root }}"
          echo "Arch: ${{ github.event.inputs.arch }} | RAM: ${{ github.event.inputs.ram_gb }}GB | CPU: ${CPU_LIMIT}"
          echo "Image: ${IMAGE_TAG}"
          echo "scrcpy: ${TUNNEL_URL:-chua co}"
          echo "Upload: ${UPLOAD_URL:-chua co}"
          echo "=========================================="

      - name: Giu job chay trong 340 phut
        run: |
          END=$((SECONDS + 340*60))
          LAST_URL=""
          LAST_UP=""
          while [ $SECONDS -lt $END ]; do
            sleep 60
            CUR=$(grep -o 'https://[a-zA-Z0-9.-]*\.trycloudflare\.com' cloudflared.log | tail -n1 || true)
            if [ -n "$CUR" ] && [ "$CUR" != "$LAST_URL" ]; then
              echo "scrcpy URL: $CUR"
              LAST_URL="$CUR"
            fi
            CUR_UP=$(grep -o 'https://[a-zA-Z0-9.-]*\.trycloudflare\.com' cloudflared-upload.log | tail -n1 || true)
            if [ -n "$CUR_UP" ] && [ "$CUR_UP" != "$LAST_UP" ]; then
              echo "upload URL: $CUR_UP"
              LAST_UP="$CUR_UP"
            fi
            echo "alive $(date)"
          done
